# Kafka
KAFKA_BROKERS=kafka:9092
TOPIC_IN=sensor-readings
TOPIC_OUT_METRICS=processed-metrics
TOPIC_OUT_ANOMALY=anomaly-detected
GROUP_ID=stream-processor

# Concurrency
WORKER_THREADS=4
QUEUE_MAXSIZE=1000

# Rules (simple thresholds for now)
TEMP_MAX=80.0
VIB_SPIKE_PCT=40
PRESS_MAX=120.0

# Idempotency (drop duplicates by eventId)
IDEMP_CACHE_MAX=100000
IDEMP_TTL_SECONDS=86400

# Health server (optional; not mandatory today)
PORT=8090